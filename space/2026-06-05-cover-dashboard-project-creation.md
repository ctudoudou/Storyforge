# Cover Dashboard Project Creation

## Goal

Mark the dashboard project creation TODO complete with explicit test coverage for the existing real local flow.

## Scope

- Verify the dashboard creates projects through `POST /api/projects`.
- Verify successful creation routes to `/project/{projectId}` instead of a mock or placeholder route.
- Update the development TODO to move the next Priority 6 item to script import.

## Verification

- Smoke tests protect the local API call and created project route target.
